# Server
PORT=4000

# MongoDB connection string
MONGODB_URI=mongodb://127.0.0.1:27017/garage

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS: comma-separated allowed origins (frontend dev server)
CORS_ORIGIN=http://localhost:5173
